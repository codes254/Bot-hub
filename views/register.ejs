<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title || 'Register' %></title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    body {
      background: url('https://i.imgur.com/FZypR60.jpeg') no-repeat center center fixed;
      background-size: cover;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
      color: #eee;
    }
    .container {
      background: rgba(0, 0, 0, 0.9); /* deep black with opacity */
      padding: 30px 40px;
      border-radius: 15px;
      box-shadow: 0 0 15px 3px #d32f2f; /* intense red glow */
      width: 100%;
      max-width: 400px;
      border: 2px solid #d32f2f; /* red border */
      backdrop-filter: blur(8px);
    }
    h1 {                                                                                                   margin-bottom: 20px;
      color: #ff3d00; /* vivid red */
      text-align: center;
      font-weight: 900;
      letter-spacing: 2px;
      text-shadow: 0 0 10px #ff3d00;
      font-family: 'Orbitron', sans-serif;
    }
    form {
      display: flex;
      flex-direction: column;
    }
    label {
      margin-bottom: 6px;
      font-weight: 700;
      color: #ccc;
      font-size: 0.9rem;
      text-shadow: 0 0 4px #d32f2f;
    }
    input[type="text"],
    input[type="password"] {
      padding: 12px 14px;
      margin-bottom: 18px;
      border: 2px solid #d32f2f;
      border-radius: 10px;
      font-size: 1rem;
      background-color: #121212;
      color: #eee;
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
      box-shadow: inset 0 0 8px #7b0000;                                                                 }
    input[type="text"]:focus,
    input[type="password"]:focus {
      outline: none;
      border-color: #ff3d00;
      box-shadow: 0 0 10px #ff3d00, inset 0 0 8px #ff3d00;
    }
    .error-list {
      margin-bottom: 15px;
      padding: 12px 16px;
      background: #440000cc;
      border: 1px solid #ff4444;
      border-radius: 10px;
      color: #ff5555;                                                                                      font-size: 0.9rem;
      list-style-type: none;                                                                               text-shadow: 0 0 4px #ff4444;
    }
    .error-list li {
      margin-bottom: 6px;
    }
    button {
      background: linear-gradient(135deg, #ff3d00, #b71c1c);
      border: none;
      padding: 14px;
      border-radius: 12px;
      color: white;
      font-weight: 900;
      font-size: 1.2rem;
      cursor: pointer;
      transition: background 0.3s ease, box-shadow 0.3s ease;
      letter-spacing: 1.5px;
      box-shadow: 0 0 15px #ff3d00;
    }
    button:hover {
      background: linear-gradient(135deg, #b71c1c, #ff3d00);
      box-shadow: 0 0 25px #ff6a4a;
    }
    .login-link {
      margin-top: 18px;
      font-size: 1rem;
      text-align: center;
      color: #bbb;
      text-shadow: 0 0 5px #d32f2f;
    }
    .login-link a {
      color: #ff3d00;
      font-weight: 700;
      text-decoration: none;
      transition: color 0.25s ease;
    }
    .login-link a:hover {
      text-decoration: underline;
      color: #ff6a4a;
      text-shadow: 0 0 8px #ff6a4a;
    }
  </style>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron&display=swap" rel="stylesheet" />
</head>
<body>
  <div class="container">
    <h1>Create Account</h1>

    <% if (errors.length > 0) { %>
      <ul class="error-list">
        <% errors.forEach(error => { %>
          <li><%= error.msg %></li>
        <% }) %>
      </ul>
    <% } %>

    <form action="/register" method="POST" novalidate>
      <label for="username">Username</label>
      <input
        type="text"
        id="username"
        name="username"
        placeholder="Your username"
        required
        minlength="3"
        value="<%= data.username ? data.username : '' %>"
        autocomplete="username"
      />

      <label for="password">Password</label>
      <input
        type="password"
        id="password"
        name="password"
        placeholder="At least 5 characters"
        required
        minlength="5"
        autocomplete="new-password"
      />

      <button type="submit">Register</button>
    </form>

    <p class="login-link">
      Already got an account? <a href="/login">Log in here</a>                                           </p>
  </div>
</body>
</html>
